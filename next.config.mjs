/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "images.pexels.com",
            "cdn.pixabay.com",
            "img.freepik.com",
        ],
    }
};


export default nextConfig;
